<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Inspection Checksheet - {{inspection.equipmentId}}</title>
    <style>
        /* Base styles for high-fidelity rendering */
        body { font-family: Arial, sans-serif; font-size: 9pt; margin: 0; padding: 0; }
        .container { width: 100%; max-width: 790px; margin: 0 auto; padding: 5mm; } 
        .header { text-align: center; margin-bottom: 10px; border-bottom: 2px solid #ddd; padding-bottom: 5px; }
        .header h1 { font-size: 14pt; margin: 0; }
        .info-box { display: flex; justify-content: space-between; border: 1px solid #000; margin-bottom: 15px; }
        .info-col { width: 33.3%; padding: 5px 8px; border-right: 1px solid #000; box-sizing: border-box; }
        .info-col:last-child { border-right: none; }
        .info-label { font-weight: bold; margin-bottom: 2px; display: block; }
        
        /* Table Styles (Checklist) */
        table { width: 99.5%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #000; padding: 4px 6px; text-align: left; vertical-align: top; font-size: 8.5pt; }
        th { background-color: #f2f2f2; text-align: center; font-weight: bold; }
        .col-no { width: 5%; text-align: center; }
        .col-condition { width: 15%; text-align: center; }
        .section-header { background-color: #dbe5f1; font-weight: bold; text-align: left; padding-left: 8px; }

        /* Finding Table Styles */
        .finding-table th { background-color: #cccccc; text-transform: uppercase; }
        .finding-table .col-finding-no { width: 5%; }
        .finding-table .col-finding-desc { width: 55%; }
        .finding-table .col-finding-status { width: 15%; text-align: center; }

        /* Signature Block */
        .signatures { display: flex; justify-content: space-between; width: 100%; margin-top: 30px; font-size: 9pt; }
        .sig-box { width: 49%; border: 1px solid #000; }
        .sig-title { text-align: center; font-weight: bold; padding: 5px; background-color: #f2f2f2; border-bottom: 1px solid #000; }
        .sig-row { display: flex; border-bottom: 1px solid #eee; }
        .sig-label, .sig-value { padding: 4px; border-right: 1px solid #ddd; width: 50%; box-sizing: border-box; }
        .sig-value:last-child { border-right: none; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="path/to/logo.png" style="height: 30px; float: left;" />
            <h1>DAILY INSPECTION CHECKSHEET COMPRESSOR</h1>
            <p>Unit: {{inspection.modelUnit}} / Location: {{inspection.location}}</p>
        </div>

        <div class="info-box">
            <div class="info-col" style="width: 25%;">
                <span class="info-label">Tanggal:</span> {{inspection.inspectionDate}}
            </div>
            <div class="info-col" style="width: 15%;">
                <span class="info-label">No. Unit:</span> {{inspection.equipmentId}}
            </div>
            <div class="info-col" style="width: 20%;">
                <span class="info-label">SMR:</span> {{inspection.smr}}
            </div>
            <div class="info-col" style="width: 20%;">
                <span class="info-label">Time Down:</span> {{inspection.timeDown}}
            </div>
            <div class="info-col" style="width: 20%; border-right: none;">
                <span class="info-label">Time Out:</span> {{inspection.timeOut}}
            </div>
        </div>

        <p style="text-align: center; margin-bottom: 10px;">
            <small>✓ = Baik | X = Rusak/Perlu Perbaikan | O = Sudah Diperbaiki | NA = Tidak Tersedia</small>
        </p>

        <table>
            <thead>
                <tr>
                    <th class="col-no">No.</th>
                    <th>KOMPONEN & ITEM PEMERIKSAAN</th>
                    <th class="col-condition">KONDISI</th>
                </tr>
            </thead>
            <tbody>
                
                <tr><td colspan="3" class="section-header">A. Mesin</td></tr>
                <tr><td class="col-no">1</td><td>Periksa level oli mesin & kebocoran</td><td class="col-condition">{{inspection.supportDetails.engineOilLevel}}</td></tr>
                <tr><td class="col-no">2</td><td>Periksa dudukan mesin</td><td class="col-condition">{{inspection.supportDetails.engineMounting}}</td></tr>
                <tr><td class="col-no">3</td><td>Periksa level air pendingin & kebocoran</td><td class="col-condition">{{inspection.supportDetails.engineCoolantLevel}}</td></tr>
                <tr><td class="col-no">4</td><td>Periksa sistem bahan bakar & kebocoran</td><td class="col-condition">{{inspection.supportDetails.engineFuelSystem}}</td></tr>
                <tr><td class="col-no">5</td><td>Periksa semua ketegangan sabuk & bagian terkait</td><td class="col-condition">{{inspection.supportDetails.engineBeltTension}}</td></tr>
                <tr><td class="col-no">6</td><td>Periksa kondisi semua filter</td><td class="col-condition">{{inspection.supportDetails.engineFilterConditions}}</td></tr>
                <tr><td class="col-no">7</td><td>Periksa pembersih udara (bersihkan jika perlu)</td><td class="col-condition">{{inspection.supportDetails.engineAirCleaner}}</td></tr>

              <tr><td colspan="3" class="section-header">B. Kelistrikan</td></tr>
<tr><td class="col-no">8</td><td>Periksa terminal dan kabel elektroda</td><td class="col-condition">{{inspection.supportDetails.electricTerminals}}</td></tr>
<tr><td class="col-no">9</td><td>Periksa semua indikator & pengukur di panel kontrol</td><td class="col-condition">{{inspection.supportDetails.electricIndicators}}</td></tr>
<tr><td class="col-no">10</td><td>Periksa level elektrolit baterai, terminal & kabel baterai</td><td class="col-condition">{{inspection.supportDetails.electricBattery}}</td></tr>
<tr><td class="col-no">11</td><td>Periksa saklar mode off, run, start</td><td class="col-condition">{{inspection.supportDetails.electricSwitchMode}}</td></tr>
<tr><td class="col-no">12</td><td>Periksa kondisi baterai & koneksi</td><td class="col-condition">{{inspection.supportDetails.electricBatteryConnection}}</td></tr>

<tr><td colspan="3" class="section-header">C. Opsional</td></tr>
<tr><td class="col-no">13</td><td>Periksa APAR</td><td class="col-condition">{{inspection.supportDetails.optionalApar}}</td></tr>

<tr><td colspan="3" class="section-header">D. Pengisian Pelumas & Pendingin</td></tr>
<tr><td class="col-no">14</td><td>Coolant</td><td class="col-condition">{{inspection.supportDetails.topUpCoolant}} L</td></tr>
<tr><td class="col-no">15</td><td>Oli Mesin (SAE 15W-40)</td><td class="col-condition">{{inspection.supportDetails.topUpEngineOil}} L</td></tr>
<tr><td class="col-no">16</td><td>Compressor</td><td class="col-condition">{{inspection.supportDetails.topUpCompressor}} L</td></tr>
                </tbody>
        </table>
        
        <table class="finding-table">
            <thead>
                <tr>
                    <th colspan="4">Finding Inspection Unit (Temuan Inspeksi)</th>
                </tr>
                <tr>
                    <th class="col-finding-no">No.</th>
                    <th class="col-finding-desc">Finding Description</th>
                    <th class="col-finding-status">Open (✓)</th>
                    <th class="col-finding-status">Close (✓)</th>
                </tr>
            </thead>
            <tbody>
                {{#each findings}}
                <tr>
                    <td class="col-finding-no">{{addOne @index}}</td> 
                    <td>{{this.description}}</td>
                    <td class="col-finding-status">{{#if (eq this.status 'open')}}✓{{/if}}</td>
                    <td class="col-finding-status">{{#if (eq this.status 'close')}}✓{{/if}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="signatures">
            <div class="sig-box">
                <div class="sig-title">Checked By ({{inspection.mechanicName}})</div>
                <div class="sig-row">
                    <div class="sig-label">Name:</div>
                    <div class="sig-value">{{inspection.mechanicName}}</div>
                </div>
            </div>
            <div class="sig-box">
                <div class="sig-title">Approved By ({{inspection.approverName}})</div>
                <div class="sig-row">
                    <div class="sig-label">Name:</div>
                    <div class="sig-value">{{inspection.approverName}}</div>
                </div>
            </div>
        </div>
        
    </div>
</body>
</html>